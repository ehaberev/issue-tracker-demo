language: node_js

node_js:
  - "8"

before_install:
  - npm install -g qunit

script:
  - npm run lint
  - npm test

deploy:
  provider: script
  skip_cleanup: true
  script: bash scripts/deploy-to-gh-pages.sh $TRAVIS_BRANCH
  on:
    all_branches: true
